import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Ensure that SVGs and other assets are handled if needed, though Next 15 handles most by default
};

export default nextConfig;
